#!/usr/bin/env sh

get_arch() {
  echo "$(uname -p)"
}

main() {
  local arch="$(get_arch)"

  poudriere logclean -y -a -j "143${arch}"
  poudriere logclean -y -a -j "150${arch}"

  poudriere pkgclean -y -A -j "143${arch}"
  poudriere pkgclean -y -A -j "150${arch}"
}

main
